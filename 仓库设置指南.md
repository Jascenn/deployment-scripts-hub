# Deployment Scripts Hub - 仓库设置指南

> 📦 从创建到发布的完整流程

---

## 概述

这份指南将帮您完成以下任务：
1. ✅ 创建 GitHub 仓库
2. ✅ 初始化 Git
3. ✅ 推送代码
4. ✅ 创建 Release
5. ✅ 测试部署

---

## 步骤 1: 创建 GitHub 仓库

### 方式 A: 使用 GitHub CLI（推荐）

```bash
cd /Users/jascen/Development/deployment-scripts-hub

# 创建仓库并推送
gh repo create deployment-scripts-hub \
    --public \
    --source=. \
    --description="一站式部署脚本仓库 - 收集各种项目的自动化部署脚本" \
    --push
```

### 方式 B: 使用 GitHub 网页

1. 访问 https://github.com/new
2. 填写信息：
   - **Repository name**: `deployment-scripts-hub`
   - **Description**: 一站式部署脚本仓库 - 收集各种项目的自动化部署脚本
   - **Public** ✓ (选择公开)
   - **不勾选** "Initialize this repository with a README"
3. 点击 "Create repository"

---

## 步骤 2: 初始化 Git 并推送

```bash
cd /Users/jascen/Development/deployment-scripts-hub

# 初始化 Git（如果还没有）
git init

# 添加所有文件
git add .

# 创建首次提交
git commit -m "Initial commit: Deployment Scripts Hub with BettaFish"

# 添加远程仓库（替换 YOUR_USERNAME）
git remote add origin https://github.com/YOUR_USERNAME/deployment-scripts-hub.git

# 推送到 GitHub
git branch -M main
git push -u origin main
```

---

## 步骤 3: 准备 BettaFish 部署包

### 3.1 返回原项目打包

```bash
cd /Users/jascen/Development/BettaFish-Deployment-Kit

# 创建完整包
tar -czf BettaFish-Deployment-Kit.tar.gz \
    --exclude='.git' \
    --exclude='旧版文档' \
    --exclude='开发说明' \
    --exclude='*.tar.gz' \
    .

# 创建最小包（如果还没有）
./create-minimal-package.sh

# 移动到新仓库
mv BettaFish-Deployment-Kit.tar.gz /Users/jascen/Development/deployment-scripts-hub/bettafish/packages/
mv BettaFish-Minimal-*.tar.gz /Users/jascen/Development/deployment-scripts-hub/bettafish/packages/BettaFish-Minimal.tar.gz
```

### 3.2 生成 SHA256 校验和

```bash
cd /Users/jascen/Development/deployment-scripts-hub/bettafish/packages

# 生成校验和
sha256sum BettaFish-Deployment-Kit.tar.gz > BettaFish-Deployment-Kit.tar.gz.sha256
sha256sum BettaFish-Minimal.tar.gz > BettaFish-Minimal.tar.gz.sha256

# 查看校验和
cat *.sha256
```

---

## 步骤 4: 创建 BettaFish Release

### 4.1 创建变更日志

```bash
cd /Users/jascen/Development/deployment-scripts-hub/bettafish

cat > CHANGELOG.md << 'EOF'
# BettaFish Deployment Kit - 变更日志

## v2.1 (2025-01-14)

### 新功能
- ✨ 智能代理配置支持
- ✨ 网络环境自动检测（国内/国外）
- ✨ 镜像清理用户确认机制
- ✨ 支持 curl 一键部署

### 优化
- 🔧 修复 ANSI 颜色显示问题
- 🔧 优化镜像清理逻辑（防止误删通用镜像）
- 📚 更新可视化文档（v2.1）
- 📚 添加完整的使用文档

### 文件说明
- `BettaFish-Deployment-Kit.tar.gz` - 完整部署包（约10MB）
  - 包含所有脚本、文档、工具
  - 适合首次部署

- `BettaFish-Minimal.tar.gz` - 最小核心包（约3MB）
  - 仅包含7个核心文件
  - 适合快速部署

- `smart-deploy.sh` - 智能部署脚本
  - 支持网络自动检测
  - 支持代理配置
  - 支持参数化部署

### 一键部署

```bash
# 完整部署
curl -fsSL https://raw.githubusercontent.com/YOUR_USERNAME/deployment-scripts-hub/main/bettafish/smart-deploy.sh | bash

# 使用代理
curl -fsSL https://raw.githubusercontent.com/YOUR_USERNAME/deployment-scripts-hub/main/bettafish/smart-deploy.sh | bash -s -- --proxy http://127.0.0.1:7890

# 最小化部署
curl -fsSL https://raw.githubusercontent.com/YOUR_USERNAME/deployment-scripts-hub/main/bettafish/smart-deploy.sh | bash -s -- --minimal
```

### 系统要求
- Docker Desktop 或 Docker Engine
- 至少 4GB RAM
- 至少 5GB 磁盘空间

### 相关链接
- 官方项目: https://github.com/666ghj/BettaFish
- 部署文档: https://github.com/YOUR_USERNAME/deployment-scripts-hub/tree/main/bettafish
- 问题反馈: https://github.com/YOUR_USERNAME/deployment-scripts-hub/issues
EOF
```

### 4.2 创建 Release

```bash
cd /Users/jascen/Development/deployment-scripts-hub

# 使用 GitHub CLI 创建 Release
gh release create bettafish-v2.1 \
    bettafish/packages/BettaFish-Deployment-Kit.tar.gz \
    bettafish/packages/BettaFish-Minimal.tar.gz \
    bettafish/packages/*.sha256 \
    bettafish/smart-deploy.sh \
    --title "BettaFish Deployment Kit v2.1" \
    --notes-file bettafish/CHANGELOG.md
```

---

## 步骤 5: 更新 URL 引用

创建 Release 后，需要更新文档中的 URL：

```bash
cd /Users/jascen/Development/deployment-scripts-hub

# 获取您的 GitHub 用户名
GITHUB_USERNAME=$(gh api user -q .login)

echo "您的 GitHub 用户名: $GITHUB_USERNAME"

# 使用 sed 替换所有 YOUR_USERNAME
find . -type f \( -name "*.md" -o -name "*.sh" \) -exec sed -i '' "s/YOUR_USERNAME/$GITHUB_USERNAME/g" {} +

# 提交更改
git add .
git commit -m "Update URLs with actual GitHub username"
git push
```

---

## 步骤 6: 测试部署

### 6.1 测试直接执行

```bash
# 测试完整部署
curl -fsSL https://raw.githubusercontent.com/$GITHUB_USERNAME/deployment-scripts-hub/main/bettafish/smart-deploy.sh | bash

# 测试帮助信息
curl -fsSL https://raw.githubusercontent.com/$GITHUB_USERNAME/deployment-scripts-hub/main/bettafish/smart-deploy.sh | bash -s -- --help
```

### 6.2 测试 Release 下载

```bash
# 测试下载完整包
curl -fsSL https://github.com/$GITHUB_USERNAME/deployment-scripts-hub/releases/download/bettafish-v2.1/BettaFish-Deployment-Kit.tar.gz -o test.tar.gz

# 验证文件完整性
curl -fsSL https://github.com/$GITHUB_USERNAME/deployment-scripts-hub/releases/download/bettafish-v2.1/BettaFish-Deployment-Kit.tar.gz.sha256
sha256sum -c test.tar.gz.sha256

# 清理测试文件
rm test.tar.gz*
```

---

## 步骤 7: 配置 Gitee 镜像（可选，国内用户推荐）

### 7.1 创建 Gitee 仓库

1. 访问 https://gitee.com/projects/new
2. 选择 "导入已有仓库"
3. 输入 GitHub 仓库地址：
   ```
   https://github.com/$GITHUB_USERNAME/deployment-scripts-hub
   ```
4. 点击 "创建"

### 7.2 配置自动同步

```bash
cd /Users/jascen/Development/deployment-scripts-hub

# 添加 Gitee 远程仓库
git remote add gitee https://gitee.com/$GITEE_USERNAME/deployment-scripts-hub.git

# 推送到 Gitee
git push gitee main

# 创建同步脚本
cat > sync-to-gitee.sh << 'EOF'
#!/bin/bash
# 同步到 Gitee

set -e

echo "同步 GitHub → Gitee..."

# 拉取最新代码
git pull origin main

# 推送到 Gitee
git push gitee main

echo "✅ 同步完成"
EOF

chmod +x sync-to-gitee.sh
```

### 7.3 手动上传 Release 到 Gitee

Gitee 不支持自动同步 Release，需要手动上传：

1. 访问 Gitee 仓库页面
2. 点击 "发行版"
3. 点击 "创建发行版"
4. 填写信息（与 GitHub Release 相同）
5. 上传文件（从 GitHub Release 下载后上传）

---

## 步骤 8: 更新 README

回到 BettaFish-Deployment-Kit 项目，添加一键部署链接：

```bash
cd /Users/jascen/Development/BettaFish-Deployment-Kit

cat > 一键部署说明.md << EOF
# BettaFish 一键部署

现在可以通过以下命令一键部署 BettaFish：

## 基础部署

\`\`\`bash
curl -fsSL https://raw.githubusercontent.com/$GITHUB_USERNAME/deployment-scripts-hub/main/bettafish/smart-deploy.sh | bash
\`\`\`

## 使用代理（国内用户推荐）

\`\`\`bash
curl -fsSL https://raw.githubusercontent.com/$GITHUB_USERNAME/deployment-scripts-hub/main/bettafish/smart-deploy.sh | bash -s -- --proxy http://127.0.0.1:7890
\`\`\`

## 最小化部署

\`\`\`bash
curl -fsSL https://raw.githubusercontent.com/$GITHUB_USERNAME/deployment-scripts-hub/main/bettafish/smart-deploy.sh | bash -s -- --minimal
\`\`\`

## 详细文档

访问: https://github.com/$GITHUB_USERNAME/deployment-scripts-hub/tree/main/bettafish
EOF

# 提交
git add 一键部署说明.md
git commit -m "Add one-line deployment instructions"
git push
```

---

## 步骤 9: 添加徽章（可选）

编辑主 README，添加状态徽章：

```bash
cd /Users/jascen/Development/deployment-scripts-hub

# 在 README.md 顶部添加徽章
# 手动编辑或使用以下命令
```

可用的徽章：
- License: `[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)`
- Maintained: `[![Maintained](https://img.shields.io/badge/Maintained-Yes-green.svg)](https://github.com/$GITHUB_USERNAME/deployment-scripts-hub)`
- GitHub Stars: `![GitHub stars](https://img.shields.io/github/stars/$GITHUB_USERNAME/deployment-scripts-hub)`
- GitHub Issues: `![GitHub issues](https://img.shields.io/github/issues/$GITHUB_USERNAME/deployment-scripts-hub)`

---

## 完成检查清单

验证所有步骤：

- [ ] GitHub 仓库已创建
- [ ] 代码已推送到 main 分支
- [ ] BettaFish v2.1 Release 已创建
- [ ] 部署包已上传（完整包 + 最小包）
- [ ] SHA256 校验和已生成
- [ ] smart-deploy.sh 可以直接 curl 执行
- [ ] URL 中的 YOUR_USERNAME 已替换
- [ ] 测试一键部署命令成功
- [ ] （可选）Gitee 镜像已配置
- [ ] （可选）徽章已添加

---

## 后续维护

### 更新脚本

```bash
cd /Users/jascen/Development/deployment-scripts-hub/bettafish

# 修改脚本
nano smart-deploy.sh

# 提交更改
git add .
git commit -m "Update BettaFish deployment script"
git push
```

### 发布新版本

```bash
# 1. 更新版本号
VERSION="v2.2"

# 2. 创建新的部署包
cd /Users/jascen/Development/BettaFish-Deployment-Kit
./create-minimal-package.sh
# 移动到 packages/

# 3. 更新 CHANGELOG.md

# 4. 创建新 Release
gh release create bettafish-$VERSION \
    bettafish/packages/BettaFish-Deployment-Kit.tar.gz \
    bettafish/packages/BettaFish-Minimal.tar.gz \
    --title "BettaFish Deployment Kit $VERSION" \
    --notes-file bettafish/CHANGELOG.md
```

---

## 常见问题

### Q: gh 命令找不到？

**A**: 安装 GitHub CLI：

```bash
# macOS
brew install gh

# 认证
gh auth login
```

### Q: git push 需要密码？

**A**: 配置 SSH 或使用 Personal Access Token：

```bash
# 使用 SSH
git remote set-url origin git@github.com:$GITHUB_USERNAME/deployment-scripts-hub.git

# 或使用 Token
git remote set-url origin https://TOKEN@github.com/$GITHUB_USERNAME/deployment-scripts-hub.git
```

### Q: 如何删除 Release？

**A**:

```bash
gh release delete bettafish-v2.1 --yes
```

---

## 总结

完成以上步骤后，您将拥有：

✅ 一个专业的部署脚本仓库
✅ BettaFish 的一键部署功能
✅ 完整的文档和示例
✅ 方便的更新和发布流程

现在可以分享给用户：

```bash
curl -fsSL https://raw.githubusercontent.com/$GITHUB_USERNAME/deployment-scripts-hub/main/bettafish/smart-deploy.sh | bash
```

---

**创建时间**: 2025-01-14
**维护者**: Claude Code
**状态**: ✅ 可以开始使用
