# Docker 离线包目录

此目录用于存放 Docker 镜像的离线包文件。

## 📦 下载离线包

运行下载脚本:

```bash
cd /Users/jascen/Development/BettaFish-Deployment-Kit
./download-offline-packages.sh
```

## 📋 应包含的文件

下载完成后，此目录应包含:

- `python-3.11-slim.tar` (~450MB) - Python 3.11 基础镜像
- `postgres-15-alpine.tar` (~250MB) - PostgreSQL 15 数据库镜像

## 🚀 使用方法

离线包会被 `docker-deploy.sh` 脚本自动检测和加载。

无需手动操作！

## 🔄 手动加载（可选）

如果需要手动加载:

```bash
docker load -i python-3.11-slim.tar
docker load -i postgres-15-alpine.tar
```

## 📁 目录大小

完整下载后约 **700MB**

---

**提示**: 如果网络良好且可以直接从 Docker Hub 拉取镜像，则不需要离线包。
